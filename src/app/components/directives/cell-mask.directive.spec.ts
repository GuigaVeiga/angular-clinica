import { CellMaskDirective } from './cell-mask.directive';

describe('CellMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CellMaskDirective();
    expect(directive).toBeTruthy();
  });
});
